import HeroSection from "../hero/HeroSection";
import Navbar from "../navbar/Navbar";

const Home = () => {
    return (
        <div>
            <Navbar/>
            <HeroSection/>
        </div>
    );
};

export default Home;